import { CodeSelector } from '@/components/code-selector.tsx'

## ::marker

`li`나 `summary` 엘리먼트의 마커를 스타일 할 때 사용할 수 있다.

<CodeSelector names={['css']} desc='marker' />

```css
li::marker {
  content: '✝ ';
  font-size: 1.2em;
}
```
