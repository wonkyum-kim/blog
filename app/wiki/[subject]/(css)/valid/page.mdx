## :valid

input 엘리먼트에 쓴 값이 valid 인지 판단하고 스타일을 적용할 수 있다.

valid를 판단하는 기준은 type, pattern, min, max로 정할 수 있다.

`type="email"` 만으로 검증하는 것은 주의해야 할 것 같다. foo@bar 까지만 써도 valid라고 판단된다.

pattern을 추가하여 추가적으로 판단해야 할 것이다.

## :invalid

valid 하지 못하면 스타일이 적용된다.
