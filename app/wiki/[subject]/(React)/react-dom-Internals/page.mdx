## ReactDOMSharedInternals.js

### ReactDOMSharedInternals

`ReactDOMSharedInternals`는 react-dom 패키지 내부에서 사용하는 객체로 현재 코드에서는 비어있는 상태로 존재한다.

- ReactDOMSharedInternals.d: TODO
- ReactDOMSharedInternals.p: currentUpdatePriority
- ReactDOMSharedInternals.findDOMNode: TODO

index.js에서 export 될 때에는 사용하지 말라는 의미에서 `__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE`라는 이름이 지어졌다.
